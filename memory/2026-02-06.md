# 2026-02-06 — Daily Notes

## 오늘의 컨텍스트
- 작업: proactive-agent 자산 문서 세트 생성(온보딩/메모리/하트비트 등)
- 레포: VisualJSON (React+TS, Zustand, AST 기반 JSON 편집기)

## 프로젝트 분석 (Raw)
- App 초기 로드: `src/App.tsx`에서 SAMPLE_JSON을 마운트 시 `jsonToAst`로 로드.
- 파일 IO: `src/hooks/useFileIO.ts`
  - open: file input으로 .json 읽기
  - save: `astToJson` 후 다운로드
  - paste: input/textarea가 아닐 때만, JSON처럼 보이면 confirm 후 교체
  - drop: .json 파일 드롭 로드
- 파서: `src/utils/parser.ts`
  - `jsonToAst`: generateId로 노드 생성, object/array는 children로 재귀
  - `astToJson`: object는 child.key 기반 재구성, array는 순서 기반
- 트리: `TreeExplorer`가 expandedIds 기반으로 DFS flatten 후 절대 위치로 렌더
- 스토어: `useJsonStore`가 document/selection/expand/search/undo/redo 및 mutations 관리

## 결정/변경
- 루트에 proactive-agent 표준 md 생성:
  - ONBOARDING.md, AGENTS.md, SOUL.md, USER.md, MEMORY.md, HEARTBEAT.md, TOOLS.md
  - memory/ 디렉토리 및 오늘 일지 생성
- Copilot 기본 지침 경로로 이전:
  - `AGENT.md` 삭제
  - `.github/copilot-instructions.md`에 해당 강제 규칙 작성

### VisualJSON 저장 UX + 타이틀
- 저장 UX 변경: 기존 `a.download` 즉시 다운로드 대신, 지원 브라우저에서는 File System Access API의 `showSaveFilePicker()`로 파일명/저장 위치를 선택.
- 기본 파일명: Open/Drag&Drop로 불러온 JSON은 원본 파일명을 `currentFileName`으로 저장해 Save 시 `suggestedName`으로 사용.
- 호환성: `showSaveFilePicker` 미지원/취소 시에는 기존 다운로드 방식으로 fallback (브라우저 보안상 다운로드 경로를 강제로 선택시키는 방식은 제공되지 않음).
- 페이지 타이틀: `document.title = "VisualJSON - [currentFileName]"`로 store 상태에 맞춰 자동 업데이트.

## 오픈 루프
- 없음 (문서 생성 완료)
